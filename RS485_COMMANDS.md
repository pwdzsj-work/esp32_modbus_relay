# RS485 命令控制模块

通信参数：Modbus RTU，从站地址 `1`，`9600 8N1`。命令模块使用保持寄存器（功能码 `03/06/10`）。

| 寄存器地址 | 属性 | 说明 |
|---:|:---:|---|
| `0x0200` | R/W | 命令参数 |
| `0x0201` | R/W | 命令触发器；写入后立即执行 |
| `0x0202` | R | 上一次执行结果 |
| `0x0203` | R | NQ5..NQ8 光耦反馈的继电器状态，bit0..3 对应继电器 1..4 |

## 命令

| 命令值 | 动作 | 参数格式 |
|---:|---|---|
| `1` | 设置单路继电器 | 低 8 位为通道 `1..4`，bit8 为开关状态 |
| `2` | 设置四路继电器 | bit0..3 为继电器状态 |
| `3` | 全部关闭 | 忽略 |
| `4` | 全部打开 | 忽略 |

执行结果：`0` 成功，`1` 命令不存在，`2` 参数错误，`3` 驱动执行失败。

使用时先写参数寄存器 `0x0200`，再写命令寄存器 `0x0201`。以下报文均已包含 Modbus CRC，CRC 低字节在前。

```text
# 打开继电器 1：参数 0x0101，然后执行命令 1
01 06 02 00 01 01 48 22
01 06 02 01 00 01 18 72

# 按位设置继电器，0x0005 表示打开 1、3：参数 5，然后执行命令 2
01 06 02 00 00 05 48 71
01 06 02 01 00 02 58 73

# 全部关闭
01 06 02 01 00 03 99 B3

# 全部打开
01 06 02 01 00 04 D8 71

# 一次读取参数、命令、结果和继电器状态
01 03 02 00 00 04 45 B1
```
